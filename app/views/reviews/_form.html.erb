<%= form_for([@product, review], url: url, class: "container", remote: true) do |f| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field col-md-12">
    <%= f.label :rating, class: "control-label" %>
    <%= f.select :rating, (1..5).to_a, class: "form-control" %>
  </div>

  <div class="field col-md-12">
    <%= f.label :title, class: "control-label" %>
    <%= f.text_field :title, class: "form-control" %>
  </div>

  <div class="field col-md-12">
    <%= f.label :text, class: "control-label" %>
    <%= f.text_area :text, class: "form-control", rows:6 %>
  </div>


  <div class="actions col-md-12">
    <%= f.submit "Save Review", class: "submit-button btn btn-success btn-block" %>
  </div>
<% end %>

