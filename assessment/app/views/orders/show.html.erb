<p id="notice"><%= notice %></p>

<p>
  <strong>Product:</strong>
  <%= link_to @order.product.title, @order.product %>
</p>

<p>
  <strong>Customer:</strong>
  <%= link_to @order.user.username, @order.user.profile %>
</p>

<p>
  <strong>Vendor:</strong>
  <%= link_to @order.product.user.username, @order.product.user.profile %>
</p>

<p>
  <strong>Quantity:</strong>
  <%= @order.quantity %>
</p>

<p>
  <strong>Price:</strong>
  <%= @order.price %>
</p>

<%= link_to "Pay Now", new_order_checkout_path(@order) %>

<%= link_to('Payment Index', orders_path) if user_signed_in? && current_user.admin? %>
